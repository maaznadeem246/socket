#ifndef SSC_CORE_TYPES_HEADERS_HH
#define SSC_CORE_TYPES_HEADERS_HH

#if !defined(SSC_INLINE_INCLUDE)
#include <any>
#include <algorithm>
#include <map>
#include <mutex>
#include <queue>
#include <regex>
#include <semaphore>
#include <string>
#include <sstream>
#include <vector>
#endif

#endif
