/**
 * @module ssc.init
 * @description TODO
 * @example
 */
export module ssc.init;
import ssc.runtime;
import ssc.types;
import ssc.dns;

using ssc::types::SharedPointer;
using ssc::runtime::Runtime;
using ssc::dns::DNS;

export namespace ssc {
}
