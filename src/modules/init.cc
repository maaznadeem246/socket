module; // global

/**
 * @module ssc.init
 * @description TODO
 * @example
 */
export module ssc.init;
import ssc.runtime;

using Runtime = ssc::runtime::Runtime;

export namespace ssc::init {
  int init (Runtime* runtime) {
  }
}
